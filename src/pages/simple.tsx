import Head from 'next/head';
import { Logo, LogoImage, LogoTitle } from '../components/StyledComponents'; // Import styled components
import ProTip from '../components/ProTip';
import SimpleLayout from '../components/Layouts/Simple/Simple'; // Import SimpleLayout

const Home: React.FC = () => {
  return (
    <SimpleLayout>
      
    </SimpleLayout>
  );
};

export default Home;
